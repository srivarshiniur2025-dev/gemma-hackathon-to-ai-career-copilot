import type { SkillFact } from "@/lib/skills/types";

/** Original interview-style items (not copied from LeetCode/GFG dumps). */
export const SKILL_FACTS: SkillFact[] = [
  { domain: "python", topic: "the difference between a list and a tuple", correct: "lists are mutable; tuples are immutable and hashable if their items are", distractors: ["tuples are faster to mutate", "lists can be dict keys", "they are identical at runtime"], why: "Immutability makes tuples hashable, so they can be dict keys; lists cannot." },
  { domain: "python", topic: "what `is` compares versus `==`", correct: "`is` tests object identity; `==` tests value equality", distractors: ["they are always the same", "`is` is for numbers only", "`==` checks memory addresses"], why: "CPython interned small ints can make `is` look like `==`, which is a trap." },
  { domain: "python", topic: "a Python decorator", correct: "a callable that wraps another callable, typically using @syntax", distractors: ["a type annotation", "a metaclass only", "a C extension"], why: "Decorators are higher-order functions used for logging, caching, auth, timing." },
  { domain: "python", topic: "GIL in CPython", correct: "it allows only one thread to execute Python bytecode at a time", distractors: ["it prevents all multiprocessing", "it makes async useless", "it locks the filesystem"], why: "CPU-bound threads do not parallelize in CPython; use multiprocessing or native extensions." },
  { domain: "python", topic: "list comprehension vs generator expression", correct: "comprehensions build a full list; generators yield lazily", distractors: ["they compile to the same bytecode", "generators use more memory", "comprehensions cannot filter"], why: "Use generators for large streams to keep memory O(1) besides the item." },
  { domain: "python", topic: "`*args` and `**kwargs`", correct: "they collect extra positional and keyword arguments", distractors: ["they are pointer syntax", "they force keyword-only args", "they disable type hints"], why: "Useful for wrappers and forward-compatible APIs." },
  { domain: "python", topic: "mutable default argument pitfall", correct: "the default object is created once and shared across calls", distractors: ["Python copies defaults each call", "only happens with ints", "it is a syntax error"], why: "Use None and create a new list/dict inside the function." },
  { domain: "python", topic: "context managers (`with`)", correct: "they guarantee setup/teardown via __enter__/__exit__ or contextlib", distractors: ["they spawn a new process", "they freeze the GIL", "they are only for files"], why: "Files, locks, and DB sessions should use with-blocks so resources close on errors." },
  { domain: "javascript", topic: "var vs let vs const", correct: "var is function-scoped and hoisted; let/const are block-scoped", distractors: ["const is immutable deeply", "let is hoisted with a usable value", "var cannot be reassigned"], why: "const prevents rebinding, not mutation of objects. Temporal dead zone applies to let/const." },
  { domain: "javascript", topic: "closures", correct: "an inner function retains access to its lexical outer scope after the outer returns", distractors: ["a function copied by value", "the same as a prototype", "only available in classes"], why: "Callbacks, partial application, and private state all rely on closures." },
  { domain: "javascript", topic: "event loop microtasks vs macrotasks", correct: "Promises/queueMicrotask run before setTimeout macrotasks", distractors: ["setTimeout always runs first", "async/await skips the queue", "the stack and queue are the same"], why: "Understanding this prevents 'why did this log first?' interview bugs." },
  { domain: "javascript", topic: "`this` in an arrow function", correct: "it lexically captures `this` from the enclosing scope", distractors: ["it is bound to the caller", "it is always window", "it is bound with call()"], why: "Arrow functions are not suitable as object methods that need dynamic this." },
  { domain: "javascript", topic: "=== versus ==", correct: "=== skips coercion; == applies abstract equality with type conversion", distractors: ["they are identical in ES2020", "== is faster", "=== compares prototypes only"], why: "Prefer ===. `0 == ''` is true; `0 === ''` is false." },
  { domain: "javascript", topic: "Promises vs async/await", correct: "async/await is syntax over Promises; rejected promises become thrown errors in await", distractors: ["async functions block the thread", "await works outside async always", "Promises cannot chain"], why: "Always handle rejections; unhandled promise rejections crash Node in strict modes." },
  { domain: "javascript", topic: "prototype vs class", correct: "ES6 class is syntactic sugar over prototype-based inheritance", distractors: ["classes create a new VM type", "prototypes cannot share methods", "extends copies all fields"], why: "Methods live on the prototype unless you assign them per instance." },
  { domain: "javascript", topic: "debouncing vs throttling", correct: "debounce waits for quiet; throttle fires at most once per interval", distractors: ["they are identical", "throttle waits for quiet", "debounce is only for scroll"], why: "Search inputs debounce; scroll handlers often throttle." },
  { domain: "dsa", topic: "time complexity of binary search on a sorted array", correct: "O(log n)", distractors: ["O(n)", "O(1)", "O(n log n)"], why: "Each step halves the search space." },
  { domain: "dsa", topic: "hash map average lookup", correct: "O(1) expected", distractors: ["O(log n) always", "O(n) always", "O(n log n)"], why: "Worst case is O(n) with collisions; good hash + resize keeps it amortized O(1)." },
  { domain: "dsa", topic: "when to use a heap", correct: "for repeated retrieve-min/max in O(log n), e.g. Dijkstra / top-k", distractors: ["for O(1) arbitrary index", "instead of a hash set", "only for sorting strings"], why: "Priority queues are heaps. Sorting once may be cheaper if you need full order." },
  { domain: "dsa", topic: "BFS vs DFS", correct: "BFS finds shortest unweighted paths; DFS is natural for graphs/trees and backtracking", distractors: ["DFS is always faster", "BFS uses no extra memory", "they visit nodes in the same order"], why: "BFS uses a queue; DFS a stack/recursion. Choose based on the question asked." },
  { domain: "dsa", topic: "stable vs unstable sort", correct: "stable sorts keep equal keys in original order", distractors: ["stable sorts are always O(n)", "unstable sorts fail on duplicates", "merge sort is unstable"], why: "Mergesort is stable; typical in-place quicksort is not. Matters for multi-key sorts." },
  { domain: "dsa", topic: "two-pointer on a sorted array", correct: "move inward based on comparison to find pairs/sums in O(n)", distractors: ["it requires a hash map", "it is O(n²) always", "it only works on trees"], why: "Classic intern interview: two-sum on sorted input without extra memory." },
  { domain: "dsa", topic: "dynamic programming hallmark", correct: "overlapping subproblems plus optimal substructure", distractors: ["only greedy choices", "random sampling", "O(1) extra space always"], why: "If subproblems do not overlap, divide-and-conquer is enough." },
  { domain: "dsa", topic: "adjacency list vs matrix", correct: "lists are better for sparse graphs; matrices for dense / O(1) edge queries", distractors: ["matrices always use less memory", "lists cannot represent directed graphs", "they have identical complexity"], why: "n=10^5, m=10^5 cannot use an n×n matrix." },
  { domain: "sql", topic: "INNER JOIN vs LEFT JOIN", correct: "INNER keeps matching rows only; LEFT keeps all left rows with NULLs on the right", distractors: ["LEFT is faster always", "INNER keeps orphans", "they ignore keys"], why: "Wrong JOIN is the #1 intern SQL bug in reports." },
  { domain: "sql", topic: "WHERE vs HAVING", correct: "WHERE filters rows before aggregation; HAVING filters groups after GROUP BY", distractors: ["HAVING is for JOINs", "WHERE can use aliases of aggregates", "they are interchangeable"], why: "COUNT(*) > 5 belongs in HAVING, not WHERE." },
  { domain: "sql", topic: "a primary key", correct: "uniquely identifies a row and is NOT NULL", distractors: ["can contain duplicates if indexed", "is the same as a foreign key", "must be an integer"], why: "Foreign keys reference primary (or unique) keys to enforce integrity." },
  { domain: "sql", topic: "normalization to 3NF", correct: "no transitive dependency of non-key attributes on the key", distractors: ["every table has one column", "all columns must be numeric", "denormalization is required"], why: "3NF reduces update anomalies; warehouses often denormalize later for reads." },
  { domain: "sql", topic: "an index tradeoff", correct: "speeds reads/filters, slows writes, uses disk", distractors: ["always speeds INSERT", "replaces WHERE", "makes DISTINCT free"], why: "Do not index every column. Index columns used in JOIN/WHERE/ORDER BY." },
  { domain: "sql", topic: "transactions ACID", correct: "Atomicity, Consistency, Isolation, Durability", distractors: ["Array, Cache, Index, Disk", "Append, Copy, Insert, Drop", "Async, Concurrent, Isolated, Durable"], why: "Intern interviews expect you to explain isolation levels at a high level." },
  { domain: "sql", topic: "NULL in SQL", correct: "unknown; comparisons with NULL yield UNKNOWN, so use IS NULL", distractors: ["NULL equals 0", "NULL equals ''", "COUNT(*) ignores nothing"], why: "COUNT(col) skips NULLs; COUNT(*) counts rows." },
  { domain: "sql", topic: "window functions", correct: "they compute over a partition without collapsing rows, e.g. ROW_NUMBER", distractors: ["they are the same as GROUP BY", "they only work in MySQL 5.1", "they update the table"], why: "Use them for ranks, running totals, and 'latest row per user'." },
  { domain: "react", topic: "why keys in lists", correct: "they help React match items across renders so state stays on the right child", distractors: ["they style the list", "index is always the best key", "keys replace ids in the DOM"], why: "Index keys break when the list reorders. Prefer stable ids." },
  { domain: "react", topic: "useEffect dependency array", correct: "it lists reactive values the effect reads; [] runs after mount", distractors: ["it disables React", "empty array runs every render", "it is required for useState"], why: "Missing deps cause stale closures; over-including can loop." },
  { domain: "react", topic: "lifting state up", correct: "move shared state to the closest common parent", distractors: ["always use Redux", "put state in CSS", "use a ref instead of state for UI"], why: "Refs do not trigger re-render. Context/Redux only when prop drilling is painful." },
  { domain: "react", topic: "controlled vs uncontrolled inputs", correct: "controlled: value + onChange in React state; uncontrolled: DOM owns the value via ref", distractors: ["uncontrolled cannot submit", "controlled skips the DOM", "they cannot mix in a form"], why: "Prefer controlled for validation; uncontrolled is fine for simple file inputs." },
  { domain: "react", topic: "memo / useMemo / useCallback", correct: "they skip extra work when inputs are referentially equal — measure first", distractors: ["they make code faster always", "useMemo replaces useState", "memo prevents all child renders"], why: "Premature memoization adds complexity. New object props still break memo." },
  { domain: "react", topic: "React Query / SWR vs useEffect fetch", correct: "they handle cache, retries, and deduping that hand-rolled effects miss", distractors: ["useEffect fetch is always better", "they replace REST", "they run on the server only"], why: "Interns often race requests and leak setState on unmount without a library." },
  { domain: "react", topic: "client vs server components (Next.js)", correct: "server components cannot use hooks or browser APIs; client components need 'use client'", distractors: ["server components run in the browser", "use client disables SSR", "hooks work in all components"], why: "Keep data fetching on the server when you can; interactivity on the client." },
  { domain: "react", topic: "reconciliation", correct: "React diffs trees by type and keys, then commits DOM updates", distractors: ["it rewrites the whole document", "it uses innerHTML always", "it skips the commit phase"], why: "Changing a component type unmounts the old tree and its state." },
  { domain: "system-design", topic: "vertical vs horizontal scaling", correct: "vertical: bigger machine; horizontal: more machines behind a load balancer", distractors: ["horizontal is always cheaper", "vertical never has limits", "they are identical in cloud"], why: "Intern-level: know why a single VM hits a ceiling." },
  { domain: "system-design", topic: "cache vs database", correct: "cache is fast ephemeral; DB is source of truth with durability", distractors: ["cache is always consistent", "DB cannot be replicated", "Redis replaces Postgres"], why: "Cache invalidation is the hard part. TTL vs write-through vs write-behind." },
  { domain: "system-design", topic: "REST vs WebSocket", correct: "REST is request/response; WebSockets are persistent full-duplex for live updates", distractors: ["REST cannot use JSON", "WebSockets are only for video", "they use different internets"], why: "Chat and interview streaming use sockets; CRUD uses REST." },
  { domain: "system-design", topic: "idempotent HTTP methods", correct: "GET, PUT, DELETE can be safely retried; POST is not idempotent by default", distractors: ["POST is always idempotent", "GET always mutates", "PATCH cannot exist"], why: "Retries after timeouts need idempotency keys for payments." },
  { domain: "system-design", topic: "what a load balancer does", correct: "distributes traffic, health-checks, and often terminates TLS", distractors: ["stores all user data", "replaces DNS", "compiles the frontend"], why: "Know round-robin vs least-connections at a high level." },
  { domain: "system-design", topic: "SQL vs NoSQL at intern level", correct: "SQL for relational integrity and joins; NoSQL for flexible docs and easy scale-out patterns", distractors: ["NoSQL cannot persist", "SQL cannot scale at all", "Mongo forbids indexes"], why: "Pick based on access patterns, not hype." },
  { domain: "system-design", topic: "CDN", correct: "edge caches static (and some dynamic) content close to users", distractors: ["a replacement for databases", "only for APIs", "a Python library"], why: "Images, JS, CSS belong on a CDN. Origin shield reduces origin load." },
  { domain: "system-design", topic: "rate limiting", correct: "caps requests per client to protect availability", distractors: ["speeds up SQL", "replaces auth", "only used in ML"], why: "Token bucket / leaky bucket come up in intern system-design screens." },
  { domain: "git", topic: "git merge vs rebase", correct: "merge preserves history with a merge commit; rebase replays commits for a linear history", distractors: ["rebase deletes remote", "merge cannot have conflicts", "they ignore branches"], why: "Do not rebase shared public history without coordination." },
  { domain: "git", topic: "detached HEAD", correct: "HEAD points at a commit, not a branch name", distractors: ["the repo is corrupted", "it means merge conflict", "Git deleted your files"], why: "Checkout a branch before committing or your work can get lost." },
  { domain: "git", topic: "git stash", correct: "temporarily shelves uncommitted changes", distractors: ["pushes to origin", "creates a tag", "rewrites authors"], why: "Useful when you must switch branches quickly." },
  { domain: "git", topic: "fast-forward merge", correct: "possible when the target has no unique commits — pointer just moves", distractors: ["always creates a merge commit", "only on tags", "requires rebase first"], why: "GitHub 'merge' vs 'squash' vs 'rebase' buttons change history shape." },
  { domain: "git", topic: ".gitignore", correct: "untracked matching files stay out of git; already-tracked files need git rm --cached", distractors: ["it deletes files from disk", "it encrypts secrets", "it applies to remotes only"], why: "Never commit .env. If already tracked, ignore is not enough." },
  { domain: "git", topic: "cherry-pick", correct: "applies a given commit onto the current branch", distractors: ["deletes a branch", "squashes all commits", "changes the remote URL"], why: "Use for hotfixes, not as a substitute for merge on long-lived branches." },
  { domain: "git", topic: "origin vs upstream", correct: "origin is your clone's default remote; upstream often names the original repo you forked", distractors: ["they are Git internals", "upstream is always GitHub", "origin cannot fetch"], why: "Fork workflow: fetch upstream, merge, push origin." },
  { domain: "git", topic: "a good commit message", correct: "imperative subject + why, not just what the diff already shows", distractors: ["must mention Jira only", "should be one word", "Git generates them always"], why: "Reviewers read history. 'fix' is not a message." },
  { domain: "ml", topic: "train/validation/test split", correct: "fit on train, tune on validation, report once on test", distractors: ["tune on test", "train on all data then split", "validation is optional always"], why: "Peeking at test is leakage. Interns fail this in take-homes." },
  { domain: "ml", topic: "overfitting", correct: "low train error, high generalization error", distractors: ["high train and test error", "a data-cleaning step", "only happens with neural nets"], why: "Regularization, more data, simpler models, dropout, early stopping." },
  { domain: "ml", topic: "precision vs recall", correct: "precision: of predicted positives, how many were right; recall: of actual positives, how many you found", distractors: ["they are the same as accuracy", "recall ignores false negatives", "precision ignores false positives"], why: "Medical screening wants recall; spam filters often want precision." },
  { domain: "ml", topic: "feature scaling", correct: "needed for distance/gradient methods (kNN, SVM, logistic, nets), not for tree splits", distractors: ["required for random forests", "only for images", "replaces normalization of labels"], why: "StandardScaler vs MinMax depends on outliers." },
  { domain: "ml", topic: "supervised vs unsupervised", correct: "supervised has labels; unsupervised finds structure without labels", distractors: ["unsupervised needs more labels", "RL is unsupervised always", "clustering is supervised"], why: "k-means is unsupervised; logistic regression is supervised." },
  { domain: "ml", topic: "cross-entropy for classification", correct: "penalizes confident wrong class probabilities", distractors: ["is only for regression", "equals MSE always", "ignores the predicted distribution"], why: "Softmax + CE is the default for multi-class nets." },
  { domain: "ml", topic: "data leakage", correct: "test information sneaks into training (scaling, target, future features)", distractors: ["using a GPU", "shuffling train only", "dropout"], why: "Fit scalers on train only. No ID columns that encode the label." },
  { domain: "ml", topic: "bias-variance tradeoff", correct: "simple models underfit (bias); overly complex models overfit (variance)", distractors: ["bias means GPU bias", "variance is always good", "they cannot be traded"], why: "You want enough capacity without memorizing noise." },
];
